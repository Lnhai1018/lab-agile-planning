# 描述性统计分析速查手册

## 📌 常用统计指标

### 集中趋势指标

| 指标 | Excel函数 | Python (Pandas) | 含义 | 适用场景 |
|-----|----------|-----------------|------|---------|
| 平均数 | `=AVERAGE(A1:A10)` | `df['列名'].mean()` | 数据的算术平均值 | 数值型数据，无极端值 |
| 中位数 | `=MEDIAN(A1:A10)` | `df['列名'].median()` | 排序后中间位置的数 | 有极端值时优于平均数 |
| 众数 | `=MODE(A1:A10)` | `df['列名'].mode()` | 出现频率最高的数 | 离散型数据 |

### 离散程度指标

| 指标 | Excel函数 | Python (Pandas) | 含义 | 说明 |
|-----|----------|-----------------|------|------|
| 极差 | `=MAX(A1:A10)-MIN(A1:A10)` | `df['列名'].max()-df['列名'].min()` | 最大值与最小值之差 | 最简单的离散度量 |
| 方差 | `=VAR(A1:A10)` | `df['列名'].var()` | 数据与均值偏差的平方和平均 | 衡量数据波动 |
| 标准差 | `=STDEV(A1:A10)` | `df['列名'].std()` | 方差的平方根 | 与原数据单位相同 |
| 变异系数 | `=STDEV(A1:A10)/AVERAGE(A1:A10)` | `df['列名'].std()/df['列名'].mean()` | 标准差与均值的比值 | 用于比较不同量纲数据 |

### 位置指标

| 指标 | Excel函数 | Python (Pandas) | 含义 |
|-----|----------|-----------------|------|
| 最小值 | `=MIN(A1:A10)` | `df['列名'].min()` | 数据的最小值 |
| 最大值 | `=MAX(A1:A10)` | `df['列名'].max()` | 数据的最大值 |
| 第一四分位数 (Q1) | `=QUARTILE(A1:A10,1)` | `df['列名'].quantile(0.25)` | 25%分位数 |
| 第三四分位数 (Q3) | `=QUARTILE(A1:A10,3)` | `df['列名'].quantile(0.75)` | 75%分位数 |
| 四分位距 (IQR) | `=QUARTILE(A1:A10,3)-QUARTILE(A1:A10,1)` | `df['列名'].quantile(0.75)-df['列名'].quantile(0.25)` | Q3 - Q1 |

### 分布形态指标

| 指标 | Python | 含义 | 解释 |
|-----|--------|------|------|
| 偏度 | `from scipy import stats`<br>`stats.skew(df['列名'])` | 数据分布的对称性 | >0 右偏，<0 左偏，=0 对称 |
| 峰度 | `stats.kurtosis(df['列名'])` | 数据分布的尖峭程度 | >0 尖峭，<0 平坦，=0 正态 |

## 📊 常用图表类型

### 图表选择指南

| 分析目的 | 推荐图表 | Excel操作 | Python代码 |
|---------|---------|----------|-----------|
| 比较数值大小 | 柱状图 | 插入→柱形图 | `plt.bar(x, y)` |
| 显示占比 | 饼图 | 插入→饼图 | `plt.pie(values, labels=labels)` |
| 展示趋势 | 折线图 | 插入→折线图 | `plt.plot(x, y)` |
| 显示分布 | 直方图 | 数据分析→直方图 | `plt.hist(data, bins=10)` |
| 识别异常值 | 箱线图 | 插入→箱线图 | `plt.boxplot(data)` |
| 显示关系 | 散点图 | 插入→散点图 | `plt.scatter(x, y)` |

### 常用Python可视化代码

#### 1. 柱状图
```python
import matplotlib.pyplot as plt

# 简单柱状图
plt.bar(categories, values, color='steelblue')
plt.xlabel('类别')
plt.ylabel('数值')
plt.title('标题')
plt.show()
```

#### 2. 饼图
```python
plt.pie(values, labels=labels, autopct='%1.1f%%', startangle=90)
plt.title('标题')
plt.axis('equal')
plt.show()
```

#### 3. 折线图
```python
plt.plot(x, y, marker='o', linewidth=2, markersize=8)
plt.xlabel('X轴')
plt.ylabel('Y轴')
plt.title('标题')
plt.grid(alpha=0.3)
plt.show()
```

#### 4. 直方图
```python
plt.hist(data, bins=15, color='skyblue', edgecolor='black')
plt.xlabel('数值范围')
plt.ylabel('频数')
plt.title('标题')
plt.show()
```

#### 5. 箱线图
```python
plt.boxplot(data, vert=True, patch_artist=True)
plt.ylabel('数值')
plt.title('标题')
plt.show()
```

#### 6. 散点图
```python
plt.scatter(x, y, s=100, alpha=0.6, c=colors)
plt.xlabel('X轴')
plt.ylabel('Y轴')
plt.title('标题')
plt.show()
```

## 🔧 Python Pandas 常用操作

### 数据导入
```python
import pandas as pd

# 读取CSV文件
df = pd.read_csv('文件名.csv', encoding='utf-8')

# 读取Excel文件
df = pd.read_excel('文件名.xlsx', sheet_name='Sheet1')
```

### 数据查看
```python
# 查看前几行
df.head()        # 默认5行
df.head(10)      # 前10行

# 查看数据信息
df.info()        # 列信息、数据类型、缺失值
df.describe()    # 数值型列的统计摘要
df.shape         # 数据形状 (行数, 列数)
df.columns       # 列名列表
```

### 数据选择
```python
# 选择单列
df['列名']
df.列名

# 选择多列
df[['列1', '列2', '列3']]

# 选择行（按索引）
df.loc[0:5]      # 按标签
df.iloc[0:5]     # 按位置

# 条件筛选
df[df['销售额'] > 100]
df[(df['销售额'] > 100) & (df['产品类别'] == '电子产品')]
```

### 数据清洗
```python
# 检查缺失值
df.isnull().sum()

# 删除缺失值
df.dropna()                    # 删除含缺失值的行
df.dropna(subset=['列名'])     # 删除指定列的缺失值

# 填充缺失值
df.fillna(0)                   # 用0填充
df.fillna(df.mean())           # 用均值填充
df['列名'].fillna(method='ffill')  # 向前填充

# 删除重复值
df.drop_duplicates()

# 替换值
df['列名'].replace('旧值', '新值')
```

### 分组统计
```python
# 单变量分组
df.groupby('产品类别')['销售额'].sum()
df.groupby('产品类别')['销售额'].mean()

# 多指标统计
df.groupby('产品类别').agg({
    '销售额': 'sum',
    '订单编号': 'count',
    '销售数量': 'mean'
})

# 多变量分组
df.groupby(['产品类别', '地区'])['销售额'].sum()
```

### 数据排序
```python
# 按单列排序
df.sort_values('销售额', ascending=False)  # 降序

# 按多列排序
df.sort_values(['产品类别', '销售额'], ascending=[True, False])
```

### 新增计算列
```python
# 简单计算
df['利润'] = df['销售额'] - df['成本']

# 条件赋值
df['等级'] = df['销售额'].apply(lambda x: '高' if x > 200 else '低')

# 分类
df['年龄段'] = pd.cut(df['年龄'], bins=[0, 20, 30, 100], 
                      labels=['青年', '中年', '老年'])
```

## 📝 Excel 数据分析技巧

### 数据透视表
1. 选择数据区域
2. 插入 → 数据透视表
3. 拖拽字段到行、列、值区域
4. 设置汇总方式（求和、计数、平均值等）

### 条件格式
1. 选择数据区域
2. 开始 → 条件格式
3. 选择规则类型（色阶、数据条、图标集）
4. 高亮显示重要数据

### 筛选和排序
1. 选择表头行
2. 数据 → 筛选
3. 点击下拉箭头进行筛选或排序

### 常用快捷键
- `Ctrl + T`: 创建表格
- `Ctrl + Shift + L`: 启用筛选
- `Alt + N + V`: 插入数据透视表
- `F2`: 编辑单元格
- `Ctrl + Home`: 跳到数据开头

## ❓ 常见问题解决

### 问题1: Python中文显示乱码
```python
# 方法1: 设置字体
plt.rcParams['font.sans-serif'] = ['SimHei']  # Windows
plt.rcParams['axes.unicode_minus'] = False

# 方法2: 保存时指定编码
df.to_csv('文件.csv', encoding='utf-8-sig')
```

### 问题2: 数据导入错误
```python
# 指定编码
df = pd.read_csv('文件.csv', encoding='utf-8')
# 或
df = pd.read_csv('文件.csv', encoding='gbk')

# 跳过错误行
df = pd.read_csv('文件.csv', error_bad_lines=False)
```

### 问题3: 日期格式转换
```python
# 转换为日期类型
df['日期'] = pd.to_datetime(df['日期'])

# 提取年月日
df['年'] = df['日期'].dt.year
df['月'] = df['日期'].dt.month
df['日'] = df['日期'].dt.day
```

### 问题4: 异常值处理
```python
# 使用IQR方法识别异常值
Q1 = df['列名'].quantile(0.25)
Q3 = df['列名'].quantile(0.75)
IQR = Q3 - Q1
lower_bound = Q1 - 1.5 * IQR
upper_bound = Q3 + 1.5 * IQR

# 筛选正常值
df_clean = df[(df['列名'] >= lower_bound) & (df['列名'] <= upper_bound)]

# 或替换异常值
df['列名'] = df['列名'].clip(lower=lower_bound, upper=upper_bound)
```

## 📚 分析报告写作模板

### 报告结构
```
一、分析背景
   - 数据来源
   - 数据时间范围
   - 数据规模
   - 分析目的

二、数据概况
   - 数据字段说明
   - 数据质量评估
   - 数据预处理方法

三、描述性统计分析
   - 整体统计指标
   - 分组统计分析
   - 分布特征分析

四、数据可视化
   - 关键图表展示
   - 图表解读

五、主要发现
   - 关键洞察1
   - 关键洞察2
   - 关键洞察3

六、结论与建议
   - 分析结论
   - 业务建议
   - 局限性说明

七、附录
   - 完整数据表
   - 代码清单
```

### 撰写技巧
1. **客观准确**: 基于数据事实，避免主观臆断
2. **简洁明了**: 使用简单语言，避免过度专业术语
3. **图文并茂**: 用图表支撑观点
4. **突出重点**: 强调关键发现和洞察
5. **提供建议**: 将分析结果转化为行动建议

## 🎯 学习检查清单

### 理论知识
- [ ] 理解均值、中位数、众数的区别
- [ ] 掌握标准差和方差的含义
- [ ] 了解偏度和峰度的意义
- [ ] 理解相关系数的解释

### 工具操作
- [ ] 能用Excel计算基本统计指标
- [ ] 能用Excel创建数据透视表
- [ ] 能用Python读取和处理CSV文件
- [ ] 能用Python绘制常用图表

### 分析能力
- [ ] 能识别数据异常值
- [ ] 能进行分组对比分析
- [ ] 能解读统计图表
- [ ] 能撰写分析报告

## 📖 推荐资源

### 在线教程
- [菜鸟教程 - Python Pandas](https://www.runoob.com/pandas/pandas-tutorial.html)
- [Matplotlib中文文档](https://www.matplotlib.org.cn/)
- [Excel教程 - Office官方](https://support.microsoft.com/zh-cn/excel)

### 数据集下载
- [Kaggle数据集](https://www.kaggle.com/datasets)
- [UCI机器学习库](https://archive.ics.uci.edu/ml/index.php)
- [阿里云天池](https://tianchi.aliyun.com/dataset)

### 实用工具
- [Google Colab](https://colab.research.google.com/) - 免费Python环境
- [Anaconda](https://www.anaconda.com/) - Python数据科学发行版
- [Jupyter Notebook](https://jupyter.org/) - 交互式编程环境

---

**持续更新中... 祝学习愉快！** 📊✨
