# 实训练习指导手册

## 练习1: 电商销售数据的描述性统计分析

### 练习目标
通过分析真实的电商销售数据，掌握描述性统计分析的完整流程。

### 数据说明
使用文件: `sample_data_sales.csv`

数据包含50条订单记录，字段说明：
- 订单编号：唯一标识
- 订单日期：2024年1-3月的日期
- 产品类别：电子产品、图书、服装、食品
- 产品名称：具体产品
- 销售数量：购买数量
- 单价：产品单价（元）
- 销售额：销售数量×单价（元）
- 客户地区：客户所在城市

### 任务步骤

#### 步骤1: 数据导入与初步检查（预计15分钟）

**使用Excel/WPS:**
1. 打开`sample_data_sales.csv`文件
2. 检查数据是否正确导入（中文是否显示正常）
3. 查看数据行数和列数
4. 检查是否有明显的缺失值或异常值

**使用Python:**
```python
import pandas as pd
import numpy as np
import matplotlib.pyplot as plt
import seaborn as sns

# 设置中文显示
plt.rcParams['font.sans-serif'] = ['SimHei']  # 用来正常显示中文标签
plt.rcParams['axes.unicode_minus'] = False  # 用来正常显示负号

# 导入数据
df = pd.read_csv('sample_data_sales.csv', encoding='utf-8')

# 查看前5行
print(df.head())

# 查看数据基本信息
print(df.info())

# 检查缺失值
print(df.isnull().sum())

# 查看数据描述性统计
print(df.describe())
```

**思考题:**
- 数据共有多少条记录？
- 有哪些数值型字段？
- 有没有缺失值？

#### 步骤2: 总体销售情况分析（预计20分钟）

**任务2.1: 计算基本统计指标**

**使用Excel:**
1. 在空白单元格计算：
   - 总销售额：`=SUM(G2:G51)`
   - 订单总数：`=COUNT(A2:A51)` 或 `=COUNTA(A2:A51)`
   - 平均订单金额：`=AVERAGE(G2:G51)`
   - 销售额中位数：`=MEDIAN(G2:G51)`
   - 销售额标准差：`=STDEV(G2:G51)`
   - 最大订单金额：`=MAX(G2:G51)`
   - 最小订单金额：`=MIN(G2:G51)`

**使用Python:**
```python
# 总销售额
total_sales = df['销售额'].sum()
print(f"总销售额: ¥{total_sales:.2f}")

# 订单数量
order_count = len(df)
print(f"订单总数: {order_count}")

# 平均订单金额
avg_order = df['销售额'].mean()
print(f"平均订单金额: ¥{avg_order:.2f}")

# 中位数
median_order = df['销售额'].median()
print(f"销售额中位数: ¥{median_order:.2f}")

# 标准差
std_order = df['销售额'].std()
print(f"销售额标准差: ¥{std_order:.2f}")

# 最大值和最小值
max_order = df['销售额'].max()
min_order = df['销售额'].min()
print(f"最大订单金额: ¥{max_order:.2f}")
print(f"最小订单金额: ¥{min_order:.2f}")
```

**任务2.2: 创建汇总表**

制作一个统计汇总表，包含以下内容：

| 统计指标 | 数值 |
|---------|------|
| 总销售额 | ? |
| 订单总数 | ? |
| 平均订单金额 | ? |
| 销售额中位数 | ? |
| 销售额标准差 | ? |
| 最大订单金额 | ? |
| 最小订单金额 | ? |

**思考题:**
- 平均值和中位数有什么差异？为什么？
- 标准差较大说明什么？

#### 步骤3: 产品类别分析（预计25分钟）

**任务3.1: 各类别销售统计**

**使用Excel:**
1. 使用数据透视表：
   - 选择数据范围
   - 插入 → 数据透视表
   - 行：产品类别
   - 值：销售额（求和）、订单编号（计数）
2. 添加计算字段：平均订单金额 = 销售额 / 订单数

**使用Python:**
```python
# 按产品类别分组统计
category_stats = df.groupby('产品类别').agg({
    '销售额': ['sum', 'mean', 'count'],
    '销售数量': 'sum'
}).round(2)

print("各类别销售统计:")
print(category_stats)

# 重命名列
category_summary = df.groupby('产品类别').agg({
    '销售额': 'sum',
    '订单编号': 'count'
}).rename(columns={'订单编号': '订单数量'})

category_summary['平均订单金额'] = (category_summary['销售额'] / 
                                  category_summary['订单数量']).round(2)
category_summary['销售额占比'] = (category_summary['销售额'] / 
                                total_sales * 100).round(2)

print("\n产品类别汇总:")
print(category_summary)
```

**任务3.2: 创建柱状图**

**使用Excel:**
1. 选择产品类别和销售额数据
2. 插入 → 柱形图 → 簇状柱形图
3. 添加数据标签和标题

**使用Python:**
```python
# 产品类别销售额柱状图
plt.figure(figsize=(10, 6))
category_sales = df.groupby('产品类别')['销售额'].sum().sort_values(ascending=False)
plt.bar(category_sales.index, category_sales.values, color='steelblue')
plt.xlabel('产品类别', fontsize=12)
plt.ylabel('销售额（元）', fontsize=12)
plt.title('各产品类别销售额对比', fontsize=14, fontweight='bold')
plt.xticks(rotation=0)

# 添加数值标签
for i, v in enumerate(category_sales.values):
    plt.text(i, v + 100, f'¥{v:.0f}', ha='center', va='bottom')

plt.tight_layout()
plt.savefig('产品类别销售对比.png', dpi=300)
plt.show()
```

**思考题:**
- 哪个类别销售额最高？
- 哪个类别订单数量最多？
- 哪个类别平均订单金额最高？

#### 步骤4: 地区分布分析（预计20分钟）

**任务4.1: 地区销售统计**

**使用Python:**
```python
# 地区销售统计
region_stats = df.groupby('客户地区').agg({
    '销售额': 'sum',
    '订单编号': 'count'
}).rename(columns={'订单编号': '订单数量'}).sort_values('销售额', ascending=False)

print("地区销售统计:")
print(region_stats)

# 创建饼图
plt.figure(figsize=(10, 8))
plt.pie(region_stats['销售额'], labels=region_stats.index, autopct='%1.1f%%',
        startangle=90, colors=plt.cm.Set3.colors)
plt.title('各地区销售额占比', fontsize=14, fontweight='bold')
plt.axis('equal')
plt.tight_layout()
plt.savefig('地区销售分布饼图.png', dpi=300)
plt.show()
```

**任务4.2: 地区订单数量对比**

```python
# 地区订单数量柱状图
plt.figure(figsize=(12, 6))
region_orders = df['客户地区'].value_counts().sort_values(ascending=False)
plt.bar(range(len(region_orders)), region_orders.values, color='coral')
plt.xlabel('城市', fontsize=12)
plt.ylabel('订单数量', fontsize=12)
plt.title('各城市订单数量对比', fontsize=14, fontweight='bold')
plt.xticks(range(len(region_orders)), region_orders.index, rotation=45)
plt.tight_layout()
plt.savefig('地区订单数量.png', dpi=300)
plt.show()
```

#### 步骤5: 销售额分布分析（预计25分钟）

**任务5.1: 绘制直方图**

```python
# 销售额分布直方图
plt.figure(figsize=(10, 6))
plt.hist(df['销售额'], bins=15, color='skyblue', edgecolor='black', alpha=0.7)
plt.xlabel('销售额（元）', fontsize=12)
plt.ylabel('频数', fontsize=12)
plt.title('订单销售额分布直方图', fontsize=14, fontweight='bold')
plt.axvline(df['销售额'].mean(), color='red', linestyle='--', 
            label=f'平均值: ¥{df["销售额"].mean():.2f}')
plt.axvline(df['销售额'].median(), color='green', linestyle='--',
            label=f'中位数: ¥{df["销售额"].median():.2f}')
plt.legend()
plt.tight_layout()
plt.savefig('销售额分布直方图.png', dpi=300)
plt.show()
```

**任务5.2: 绘制箱线图**

```python
# 销售额箱线图
plt.figure(figsize=(8, 6))
plt.boxplot(df['销售额'], vert=True, patch_artist=True,
            boxprops=dict(facecolor='lightblue'))
plt.ylabel('销售额（元）', fontsize=12)
plt.title('订单销售额箱线图', fontsize=14, fontweight='bold')
plt.grid(axis='y', alpha=0.3)
plt.tight_layout()
plt.savefig('销售额箱线图.png', dpi=300)
plt.show()

# 计算四分位数
q1 = df['销售额'].quantile(0.25)
q2 = df['销售额'].quantile(0.50)
q3 = df['销售额'].quantile(0.75)
iqr = q3 - q1

print(f"第一四分位数 (Q1): ¥{q1:.2f}")
print(f"第二四分位数 (Q2/中位数): ¥{q2:.2f}")
print(f"第三四分位数 (Q3): ¥{q3:.2f}")
print(f"四分位距 (IQR): ¥{iqr:.2f}")
```

**任务5.3: 计算偏度和峰度**

```python
from scipy import stats

# 计算偏度和峰度
skewness = stats.skew(df['销售额'])
kurtosis = stats.kurtosis(df['销售额'])

print(f"\n销售额分布特征:")
print(f"偏度 (Skewness): {skewness:.3f}")
print(f"峰度 (Kurtosis): {kurtosis:.3f}")

# 解释
if skewness > 0:
    print("数据呈正偏（右偏），高值较多")
elif skewness < 0:
    print("数据呈负偏（左偏），低值较多")
else:
    print("数据近似对称分布")
```

#### 步骤6: 时间趋势分析（预计25分钟）

**任务6.1: 转换日期格式并按月统计**

```python
# 转换日期格式
df['订单日期'] = pd.to_datetime(df['订单日期'])
df['月份'] = df['订单日期'].dt.to_period('M')

# 按月统计
monthly_sales = df.groupby('月份').agg({
    '销售额': 'sum',
    '订单编号': 'count'
}).rename(columns={'订单编号': '订单数量'})

print("月度销售统计:")
print(monthly_sales)

# 计算环比增长率
monthly_sales['销售额环比'] = monthly_sales['销售额'].pct_change() * 100
print("\n含环比增长率:")
print(monthly_sales)
```

**任务6.2: 绘制时间趋势图**

```python
# 月度销售额趋势图
fig, ax1 = plt.subplots(figsize=(12, 6))

# 销售额折线图
color = 'tab:blue'
ax1.set_xlabel('月份', fontsize=12)
ax1.set_ylabel('销售额（元）', color=color, fontsize=12)
ax1.plot(monthly_sales.index.astype(str), monthly_sales['销售额'], 
         color=color, marker='o', linewidth=2, markersize=8, label='销售额')
ax1.tick_params(axis='y', labelcolor=color)
ax1.grid(alpha=0.3)

# 订单数量柱状图（共用x轴）
ax2 = ax1.twinx()
color = 'tab:orange'
ax2.set_ylabel('订单数量', color=color, fontsize=12)
ax2.bar(monthly_sales.index.astype(str), monthly_sales['订单数量'], 
        color=color, alpha=0.3, label='订单数量')
ax2.tick_params(axis='y', labelcolor=color)

plt.title('月度销售趋势分析', fontsize=14, fontweight='bold')
fig.tight_layout()
plt.savefig('月度销售趋势.png', dpi=300)
plt.show()
```

#### 步骤7: 单价与销售数量分析（预计20分钟）

**任务7.1: 单价分布分析**

```python
# 产品单价描述性统计
print("产品单价统计:")
print(df['单价'].describe())

# 按产品类别统计单价
price_by_category = df.groupby('产品类别')['单价'].agg(['mean', 'min', 'max']).round(2)
print("\n各类别产品单价统计:")
print(price_by_category)
```

**任务7.2: 单价与销售数量关系**

```python
# 散点图：单价 vs 销售数量
plt.figure(figsize=(10, 6))
scatter = plt.scatter(df['单价'], df['销售数量'], 
                     c=df['销售额'], cmap='viridis', 
                     s=100, alpha=0.6, edgecolors='black')
plt.xlabel('单价（元）', fontsize=12)
plt.ylabel('销售数量', fontsize=12)
plt.title('产品单价与销售数量关系', fontsize=14, fontweight='bold')
plt.colorbar(scatter, label='销售额（元）')
plt.grid(alpha=0.3)
plt.tight_layout()
plt.savefig('单价销售数量关系.png', dpi=300)
plt.show()

# 计算相关系数
correlation = df['单价'].corr(df['销售数量'])
print(f"\n单价与销售数量的相关系数: {correlation:.3f}")
```

#### 步骤8: 综合报告撰写（预计30分钟）

**报告结构建议:**

```
一、分析背景
  - 数据来源和时间范围
  - 数据规模（50条订单）
  - 分析目的

二、整体销售情况
  - 总销售额: ¥X
  - 订单总数: X
  - 平均订单金额: ¥X
  - 关键发现

三、产品类别分析
  - 各类别销售表现
  - 类别占比
  - 附图表

四、地区分布分析
  - 主要销售地区
  - 地区差异
  - 附图表

五、销售额分布特征
  - 分布形态
  - 集中趋势和离散程度
  - 附直方图和箱线图

六、时间趋势分析
  - 月度变化
  - 环比增长
  - 趋势说明

七、结论与建议
  - 主要发现总结
  - 数据洞察
  - 业务建议
```

**完整分析代码示例（整合版）:**

```python
import pandas as pd
import numpy as np
import matplotlib.pyplot as plt
import seaborn as sns
from scipy import stats

# 设置样式
plt.style.use('seaborn-v0_8-darkgrid')
plt.rcParams['font.sans-serif'] = ['SimHei']
plt.rcParams['axes.unicode_minus'] = False

# 1. 数据导入
df = pd.read_csv('sample_data_sales.csv', encoding='utf-8')
df['订单日期'] = pd.to_datetime(df['订单日期'])

# 2. 创建综合分析报告
print("="*60)
print("电商销售数据描述性统计分析报告")
print("="*60)

# 3. 整体统计
print("\n一、整体销售情况")
print("-"*60)
total_sales = df['销售额'].sum()
order_count = len(df)
avg_order = df['销售额'].mean()
median_order = df['销售额'].median()
std_order = df['销售额'].std()

print(f"数据时间范围: {df['订单日期'].min().date()} 至 {df['订单日期'].max().date()}")
print(f"订单总数: {order_count}")
print(f"总销售额: ¥{total_sales:,.2f}")
print(f"平均订单金额: ¥{avg_order:.2f}")
print(f"销售额中位数: ¥{median_order:.2f}")
print(f"销售额标准差: ¥{std_order:.2f}")
print(f"变异系数: {(std_order/avg_order)*100:.2f}%")

# 4. 产品类别分析
print("\n二、产品类别分析")
print("-"*60)
category_summary = df.groupby('产品类别').agg({
    '销售额': 'sum',
    '订单编号': 'count'
}).rename(columns={'订单编号': '订单数量'})
category_summary['平均订单金额'] = (category_summary['销售额'] / 
                                  category_summary['订单数量']).round(2)
category_summary['销售额占比(%)'] = (category_summary['销售额'] / 
                                    total_sales * 100).round(2)
category_summary = category_summary.sort_values('销售额', ascending=False)
print(category_summary)

# 5. 地区分析
print("\n三、地区分布分析")
print("-"*60)
region_summary = df.groupby('客户地区').agg({
    '销售额': 'sum',
    '订单编号': 'count'
}).rename(columns={'订单编号': '订单数量'}).sort_values('销售额', ascending=False)
print(region_summary.head(10))

# 6. 分布特征
print("\n四、销售额分布特征")
print("-"*60)
print(f"Q1 (25%分位数): ¥{df['销售额'].quantile(0.25):.2f}")
print(f"Q2 (50%分位数/中位数): ¥{df['销售额'].quantile(0.50):.2f}")
print(f"Q3 (75%分位数): ¥{df['销售额'].quantile(0.75):.2f}")
print(f"IQR (四分位距): ¥{df['销售额'].quantile(0.75) - df['销售额'].quantile(0.25):.2f}")
print(f"偏度: {stats.skew(df['销售额']):.3f}")
print(f"峰度: {stats.kurtosis(df['销售额']):.3f}")

# 7. 时间趋势
print("\n五、时间趋势分析")
print("-"*60)
df['月份'] = df['订单日期'].dt.to_period('M')
monthly_sales = df.groupby('月份').agg({
    '销售额': 'sum',
    '订单编号': 'count'
}).rename(columns={'订单编号': '订单数量'})
monthly_sales['环比增长(%)'] = monthly_sales['销售额'].pct_change() * 100
print(monthly_sales)

print("\n分析完成！")
```

### 练习要求

1. **必须完成**:
   - 完成所有7个步骤的数据分析
   - 生成至少5张图表
   - 撰写完整分析报告（1000字以上）

2. **报告提交格式**:
   - Word文档或PDF
   - 包含所有图表
   - 附带Python代码或Excel文件

3. **评分标准**:
   - 数据处理正确性（30分）
   - 统计分析准确性（30分）
   - 图表质量（20分）
   - 报告完整性和洞察力（20分）

---

## 练习2: 课程满意度调查数据分析

### 练习目标
分析学员满意度数据，理解分组统计和相关性分析。

### 数据说明
使用文件: `sample_data_survey.csv`

数据包含30名学员的课程评价，字段说明：
- 学员编号：唯一标识
- 年龄：学员年龄
- 性别：男/女
- 课程评分：1-5分
- 讲师评分：1-5分
- 平台评分：1-5分
- 推荐意愿：1-5分
- 学习时长：累计学习小时数

### 任务清单

#### 任务1: 整体满意度分析
- 计算各评分维度的均值、标准差
- 绘制各维度评分的箱线图
- 识别评分最高和最低的维度

#### 任务2: 按性别分组分析
- 对比男女学员的评分差异
- 绘制分组对比柱状图
- 分析是否存在显著差异

#### 任务3: 按年龄段分组分析
- 将年龄分为三组：20-21岁、22-23岁、24岁以上
- 对比不同年龄段的满意度
- 分析年龄对满意度的影响

#### 任务4: 学习时长与满意度关系
- 绘制学习时长与各评分的散点图
- 计算相关系数
- 分析学习时长是否影响满意度

#### 任务5: 综合评分计算
- 计算综合满意度（各维度加权平均）
- 识别满意度最高和最低的学员
- 提出改进建议

### Python代码框架

```python
import pandas as pd
import matplotlib.pyplot as plt
import seaborn as sns
import numpy as np

# 设置中文显示
plt.rcParams['font.sans-serif'] = ['SimHei']
plt.rcParams['axes.unicode_minus'] = False

# 读取数据
df = pd.read_csv('sample_data_survey.csv', encoding='utf-8')

# 任务1: 整体统计
print("各维度评分统计:")
score_columns = ['课程评分', '讲师评分', '平台评分', '推荐意愿']
print(df[score_columns].describe())

# 任务2: 按性别分组
gender_stats = df.groupby('性别')[score_columns].mean()
print("\n按性别分组统计:")
print(gender_stats)

# 任务3: 按年龄段分组
df['年龄段'] = pd.cut(df['年龄'], bins=[19, 22, 24, 27], 
                      labels=['20-21岁', '22-23岁', '24岁以上'])
age_stats = df.groupby('年龄段')[score_columns].mean()
print("\n按年龄段分组统计:")
print(age_stats)

# 任务4: 相关性分析
print("\n学习时长与各评分的相关系数:")
for col in score_columns:
    corr = df['学习时长'].corr(df[col])
    print(f"{col}: {corr:.3f}")

# 任务5: 综合评分
df['综合满意度'] = (df['课程评分'] * 0.3 + 
                   df['讲师评分'] * 0.3 + 
                   df['平台评分'] * 0.2 + 
                   df['推荐意愿'] * 0.2)
print("\n综合满意度统计:")
print(df['综合满意度'].describe())

# TODO: 添加更多分析和可视化
```

### 思考题

1. 哪个评分维度的标准差最大？这说明什么？
2. 男女学员的满意度是否有明显差异？
3. 学习时长与满意度呈正相关还是负相关？
4. 如何提升整体课程满意度？

---

## 练习3: 自主项目（挑战）

### 项目要求

选择以下主题之一，完成完整的数据分析项目：

1. **电商产品评论分析**
   - 收集某产品的30-50条评论数据
   - 分析评分分布、评论长度、关键词等
   - 识别用户关注点

2. **校园食堂满意度调查**
   - 设计问卷并收集30份数据
   - 分析食堂各方面表现
   - 提出改进建议

3. **个人时间管理数据分析**
   - 记录一个月的时间使用数据
   - 分析时间分配模式
   - 优化时间管理

### 项目流程

1. **数据收集**（第1周）
   - 确定数据来源
   - 收集或生成数据
   - 确保数据质量

2. **数据分析**（第2周）
   - 数据清洗
   - 描述性统计
   - 多维度分析
   - 可视化呈现

3. **报告撰写**（第3周）
   - 撰写分析报告
   - 制作演示文稿
   - 准备答辩

### 提交内容

1. 原始数据文件
2. 分析代码（Python或Excel）
3. 分析报告（2000字以上）
4. 演示文稿（10页左右）
5. 项目总结和反思

---

## 学习资源推荐

### Python学习
- [Python官方教程](https://docs.python.org/zh-cn/3/tutorial/)
- [Pandas官方文档](https://pandas.pydata.org/docs/)
- [Matplotlib教程](https://matplotlib.org/stable/tutorials/index.html)

### 统计学习
- 可汗学院统计学课程
- Coursera统计学专项课程
- 《统计学》教材配套资源

### 实践平台
- Kaggle Learn（数据分析入门）
- DataCamp（交互式学习）
- Google Colab（免费Python环境）

祝学习顺利！有问题随时提问。
